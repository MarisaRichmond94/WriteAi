# Phases 2-3 (parser, chunker, extractor) run on the standard library alone.
# Dependencies below are introduced by later phases; install with:
#   pip install -r requirements.txt

# Phase 3+ — LLM metadata extraction and query answering
anthropic>=0.40

# Phase 4+ — vector store and embeddings
chromadb>=0.5
sentence-transformers>=3.0     # only needed if EMBEDDING_PROVIDER=local
einops>=0.8                    # required by the nomic embedding models
# openai>=1.40                 # only needed if EMBEDDING_PROVIDER=openai

# Phase 7 — settings TUI
# questionary>=2.0

# Optional: PDF text extraction fallback (.pdf sources or .pages QuickLook previews)
# pypdf>=4.0
